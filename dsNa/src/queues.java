import java.util.LinkedList;
import java.util.Queue;

public class queues {
    //first in first out, linear data structure
    public static void main(String[] args) {
        Queue<String> queue = new LinkedList<>();

        /*
        queue.offer("person1");
        queue.offer("person2");
        prints - person1, person2
        queue.peek(); prints person1
        queue.pull(); removes person1
        queue.isEmpty(); returns true or false
        queue.size() returns size of queue starting at 1
        queue.contains("person2"); returns true or false

        uses.
        1. keyboard buffer (letters should appear on the screen in the order they're pressed)
        2. printer queue (print jobs should be completed in order)
        3. Used in linked lists, priority ques, breadth-first search algorithms
        */
    }
}
