import java.util.Stack;

public class stacks {
    //last in first out
    public static void main(String[] args) {
        Stack<String> stack = new Stack<>();
        /*System.out.println(stack.empty());
        stack.push("Minecraft");
        stack.push("league");
        stack.pop();  will pop out the last added in stack ie league
        stack.peek();  gets object at top of stack ie league
        stack.search("Minecraft");  uses finds indexes starting at 1 ie 2
        if adding to many to stacks, can run out of space on the heap
        uses.
        1. undo/redo features in text editors
        2. moving back/forward through browser history
        3. backtracking algorithms (maze, file directories)
        4. calling functions (call stack)
         */
    }
}
